name: coleta-qpf
on:
  schedule:
    - cron: '10 10 * * *'
    - cron: '10 21 * * *'
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  coletar:
    runs-on: ubuntu-latest
    env:
      TZ: America/Sao_Paulo
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - run: pip install requests
      - run: python3 coletor_qpf.py
      - run: |
          git config user.name "coleta-qpf"
          git config user.email "actions@users.noreply.github.com"
          git add chuva_prevista/
          git commit -m "qpf $(date +%F_%H%M)" || true
          git push
